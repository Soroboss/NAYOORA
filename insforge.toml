[auth]
# Replace with the final Vercel production URL before `config apply`.
allowed_redirect_urls = [
  "http://localhost:3000/**",
  "https://nayoora.vercel.app/**"
]

[auth.email]
enable_confirmations = true

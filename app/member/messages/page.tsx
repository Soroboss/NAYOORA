import{memberContext}from'@/lib/member-portal';export default async function Messages(){const{s,m}=await memberContext();const{data}=await s.from('notification_inbox').select('id,title,body,read_at,created_at').eq('organization_member_id',m.id).eq('organization_id',m.organization_id).order('created_at',{ascending:false});return <section className="member-content"><p className="eyebrow">Messages</p><h1>Mes notifications</h1><div className="finance-list">{data?.map(n=><div key={n.id}><span><b>{n.title}</b><small>{n.body} · {new Date(n.created_at).toLocaleString('fr-FR')}</small></span><b>{n.read_at?'Lu':'Nouveau'}</b></div>)}</div></section>}

import Link from'next/link';import{memberContext}from'@/lib/member-portal';export default async function MemberHome(){const{s,m,profile}=await memberContext();if(!profile)return <section className="member-empty"><h1>Profil membre à relier</h1><p>Demandez à l’administrateur de rattacher votre compte utilisateur à votre fiche membre.</p></section>;const[{data:contributions},{data:events},{data:notes}]=await Promise.all([s.from('contributions').select('amount_due,amount_paid,status').eq('member_profile_id',profile.id).eq('organization_id',m.organization_id),s.from('event_attendance').select('id').eq('member_profile_id',profile.id).eq('organization_id',m.organization_id),s.from('notification_inbox').select('id').eq('organization_member_id',m.id).is('read_at',null)]);const due=(contributions??[]).reduce((x,c)=>x+Number(c.amount_due)-Number(c.amount_paid),0);return <section className="member-content"><p className="eyebrow">{(m.organization as any)?.name}</p><h1>Bonjour {profile.first_name} 👋</h1><p>Votre espace personnel rassemble l’essentiel de votre vie de membre.</p><div className="finance-stats"><article><p>Cotisations à régler</p><strong>{due.toLocaleString('fr-FR')} FCFA</strong></article><article><p>Événements suivis</p><strong>{events?.length??0}</strong></article><article><p>Nouveaux messages</p><strong>{notes?.length??0}</strong></article></div><div className="member-links"><Link href="/member/contributions">Voir mes cotisations →</Link><Link href="/member/card">Afficher ma carte →</Link><Link href="/member/messages">Lire mes messages →</Link></div></section>}

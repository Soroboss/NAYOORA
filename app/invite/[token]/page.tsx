"use client";import{useParams,useRouter}from'next/navigation';import{useState}from'react';export default function Invite(){const{token}=useParams<{token:string}>();const r=useRouter(),[n,setN]=useState(''),[busy,setBusy]=useState(false);async function accept(){setBusy(true);const x=await fetch('/api/access',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'accept',token})}),d=await x.json();setBusy(false);if(!x.ok)return setN(d.error);setN('Invitation acceptée. Votre espace est prêt.');setTimeout(()=>r.push('/dashboard'),800)}return <main className="auth-page"><section className="auth-card"><p className="eyebrow">NAYOORA</p><h1>Rejoindre une organisation</h1><p>Acceptez cette invitation avec votre compte connecté.</p><button className="button button-dark full" disabled={busy} onClick={accept}>{busy?'Vérification…':'Accepter l’invitation'}</button>{n&&<p className="form-error">{n}</p>}</section></main>}

import Link from'next/link';import{redirect}from'next/navigation';import{createClient}from'@/lib/supabase/server';import{SolidarityManager}from'@/components/solidarity-manager';
export default async function Solidarity(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const{data:m}=await s.from('organization_members').select('organization_id,role,organization:organizations(name)').eq('user_id',user.id).eq('status','active').limit(1).maybeSingle();if(!m)redirect('/onboarding');const o=m.organization_id;const[a,b,c]=await Promise.all([s.from('member_profiles').select('id,first_name,last_name').eq('organization_id',o).is('deleted_at',null).eq('status','active').order('last_name'),s.from('solidarity_cases').select('id,title,case_type,requested_amount,approved_amount,status,member:member_profiles(first_name,last_name)').eq('organization_id',o).order('requested_at',{ascending:false}),s.from('disbursements').select('id,amount,disbursed_at,solidarity_case:solidarity_cases(title)').eq('organization_id',o).not('solidarity_case_id','is',null).order('disbursed_at',{ascending:false}).limit(8)]);return <main className="app-shell"><aside className="sidebar"><Link href="/dashboard" className="brand"><img src="/nayoora-logo.png" alt="" /> NAYOORA</Link><div className="org-switch"><span>♥</span><div><b>{(m.organization as any)?.name}</b><small>Solidarité</small></div></div><nav><Link href="/dashboard">◈ Vue d'ensemble</Link><Link href="/dashboard/members">· Membres</Link><Link href="/dashboard/finance">· Cotisations & Caisse</Link><Link href="/dashboard/credit">· Prêts & Créances</Link><Link className="active" href="/dashboard/solidarity">· Solidarité</Link></nav></aside><section className="dashboard"><header className="dashboard-header"><div><p className="eyebrow">Fonds de solidarité</p><h1>Soutiens & décaissements</h1><p>Accompagnez les membres tout en gardant une trace financière claire.</p></div></header><SolidarityManager members={a.data??[]} cases={b.data??[]} disbursements={c.data??[]} canManage={["organization_admin","president","tresorier"].includes(m.role)}/></section></main>}

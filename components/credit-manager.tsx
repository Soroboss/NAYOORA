"use client";import{FormEvent,useMemo,useState}from'react';const f=(n:number)=>new Intl.NumberFormat('fr-FR',{style:'currency',currency:'XOF',maximumFractionDigits:0}).format(n);async function send(body:object){const r=await fetch('/api/credit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function CreditManager({members,loans,debts,canManage}:{members:any[];loans:any[];debts:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false);const active=loans.filter(x=>['disbursed','repaying'].includes(x.status));const total=useMemo(()=>debts.reduce((x,d)=>x+Number(d.amount_outstanding),0),[debts]);async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{await send({action,...Object.fromEntries(new FormData(e.currentTarget))});setN('Opération enregistrée. Actualisez la page pour voir les montants recalculés.');e.currentTarget.reset()}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace"><div className="finance-stats"><article><p>Prêts actifs</p><strong>{active.length}</strong></article><article><p>Capital prêté</p><strong>{f(active.reduce((s,l)=>s+Number(l.principal),0))}</strong></article><article><p>Créances ouvertes</p><strong>{f(total)}</strong></article></div>{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'loan')}><p className="eyebrow">Prêts</p><h2>Décaisser un prêt</h2><select name="memberId" required><option value="">Choisir un membre</option>{members.map(m=><option key={m.id} value={m.id}>{m.first_name} {m.last_name}</option>)}</select><input required name="principal" type="number" min="1" placeholder="Montant FCFA"/><input required name="duration" type="number" min="1" placeholder="Durée (mois)"/><input name="interest" type="number" min="0" placeholder="Taux d’intérêt (%)"/><button disabled={busy} className="button button-dark">Décaisser</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'repayment')}><p className="eyebrow">Remboursements</p><h2>Enregistrer un remboursement</h2><select name="loanId" required><option value="">Choisir un prêt actif</option>{active.map(l=><option key={l.id} value={l.id}>{l.member?.first_name} {l.member?.last_name} — {f(l.principal)}</option>)}</select><input required name="principal" type="number" min="1" placeholder="Capital remboursé FCFA"/><input name="interest" type="number" min="0" placeholder="Intérêts reçus FCFA"/><input name="notes" placeholder="Référence ou note"/><button disabled={busy} className="button button-dark">Confirmer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'debt')}><p className="eyebrow">Créances</p><h2>Créer une créance</h2><select name="memberId" required><option value="">Choisir un membre</option>{members.map(m=><option key={m.id} value={m.id}>{m.first_name} {m.last_name}</option>)}</select><input required name="amount" type="number" min="1" placeholder="Montant FCFA"/><input name="dueDate" type="date"/><input name="source" placeholder="Motif de la créance"/><button disabled={busy} className="button button-dark">Ajouter</button></form></div>}{n&&<p className="member-message">{n}</p>}<div className="finance-lists"><article className="panel"><p className="eyebrow">Prêts</p><h2>Portefeuille</h2><div className="finance-list">{loans.map(l=><div key={l.id}><span><b>{l.member?.first_name} {l.member?.last_name}</b><small>{l.duration_months} mois · {l.interest_rate}% · {l.status}</small></span><b>{f(l.principal)}</b></div>)}</div></article><article className="panel"><p className="eyebrow">Créances</p><h2>À recouvrer</h2><div className="finance-list">{debts.map(d=><div key={d.id}><span><b>{d.member?.first_name} {d.member?.last_name}</b><small>Échéance {d.due_date||'non définie'}</small></span><b>{f(d.amount_outstanding)}</b></div>)}</div></article></div></div>}

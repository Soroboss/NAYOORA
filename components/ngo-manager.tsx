"use client";import{FormEvent,useState}from'react';async function send(x:object){const r=await fetch('/api/ngo',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function NgoManager({projects,beneficiaries,donors,canManage}:{projects:any[];beneficiaries:any[];donors:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false);async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{await send({action,...Object.fromEntries(new FormData(e.currentTarget))});setN('Enregistrement effectué. Actualisez la page pour actualiser les listes.');e.currentTarget.reset()}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace"><div className="finance-stats"><article><p>Projets</p><strong>{projects.length}</strong></article><article><p>Bénéficiaires</p><strong>{beneficiaries.length}</strong></article><article><p>Donateurs</p><strong>{donors.length}</strong></article></div>{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'project')}><p className="eyebrow">Projets</p><h2>Créer un projet</h2><input name="code" placeholder="Code projet"/><input required name="name" placeholder="Nom du projet"/><select name="status"><option value="draft">Brouillon</option><option value="active">Actif</option><option value="paused">En pause</option></select><input name="startsAt" type="date"/><button disabled={busy} className="button button-dark">Créer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'beneficiary')}><p className="eyebrow">Bénéficiaires</p><h2>Ajouter une personne</h2><input required name="name" placeholder="Nom complet"/><input name="phone" placeholder="Téléphone"/><select name="projectId"><option value="">Sans projet assigné</option>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input name="category" placeholder="Catégorie / profil"/><button disabled={busy} className="button button-dark">Ajouter</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'donor')}><p className="eyebrow">Donateurs</p><h2>Ajouter un donateur</h2><input required name="name" placeholder="Nom ou organisation"/><select required name="type"><option value="">Type</option><option value="individual">Particulier</option><option value="company">Entreprise</option><option value="institution">Institution</option></select><input name="email" type="email" placeholder="Email"/><input name="phone" placeholder="Téléphone"/><button disabled={busy} className="button button-dark">Ajouter</button></form></div>}{n&&<p className="member-message">{n}</p>}<div className="finance-lists"><article className="panel"><p className="eyebrow">Projets</p><h2>Portefeuille</h2><div className="finance-list">{projects.map(p=><div key={p.id}><span><b>{p.name}</b><small>{p.code||'Sans code'} · {p.starts_at||'Date à définir'}</small></span><b>{p.status}</b></div>)}</div></article><article className="panel"><p className="eyebrow">Donateurs</p><h2>Partenaires</h2><div className="finance-list">{donors.map(d=><div key={d.id}><span><b>{d.name}</b><small>{d.email||d.phone||'Sans contact'}</small></span><b>{d.donor_type}</b></div>)}</div></article></div><article className="panel"><p className="eyebrow">Bénéficiaires</p><h2>Personnes accompagnées</h2><div className="finance-list">{beneficiaries.map(b=><div key={b.id}><span><b>{b.full_name}</b><small>{b.project?.name||'Sans projet'} · {b.phone||'Sans contact'}</small></span></div>)}</div></article></div>}

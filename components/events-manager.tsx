"use client";import{FormEvent,useMemo,useState}from'react';async function send(x:object){const r=await fetch('/api/events',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function EventsManager({members,events,attendance,canManage}:{members:any[];events:any[];attendance:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false),[chosen,setChosen]=useState<string[]>([]);const counts=useMemo(()=>Object.fromEntries(events.map(e=>[e.id,attendance.filter(a=>a.event_id===e.id&&a.status==='attended').length])),[events,attendance]);async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{const d=Object.fromEntries(new FormData(e.currentTarget));if(action==='invite')Object.assign(d,{memberIds:chosen});await send({action,...d});setN(action==='invite'?'Convocations enregistrées.':'Opération enregistrée. Actualisez la page pour actualiser la liste.');e.currentTarget.reset();if(action==='invite')setChosen([])}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace"><div className="finance-stats"><article><p>Événements</p><strong>{events.length}</strong></article><article><p>À venir</p><strong>{events.filter(e=>new Date(e.starts_at)>new Date()).length}</strong></article><article><p>Présences enregistrées</p><strong>{attendance.filter(a=>a.status==='attended').length}</strong></article></div>{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'event')}><p className="eyebrow">Événements</p><h2>Planifier</h2><input required name="title" placeholder="Ex. Assemblée générale"/><input name="location" placeholder="Lieu"/><input required name="startsAt" type="datetime-local"/><input name="endsAt" type="datetime-local"/><button disabled={busy} className="button button-dark">Créer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'invite')}><p className="eyebrow">Convocations</p><h2>Inviter les membres</h2><select name="eventId" required><option value="">Choisir un événement</option>{events.map(e=><option key={e.id} value={e.id}>{e.title}</option>)}</select><div className="member-picker">{members.map(m=><label key={m.id}><input type="checkbox" checked={chosen.includes(m.id)} onChange={()=>setChosen(chosen.includes(m.id)?chosen.filter(x=>x!==m.id):[...chosen,m.id])}/>{m.first_name} {m.last_name}</label>)}</div><button disabled={busy||!chosen.length} className="button button-dark">Convoquer ({chosen.length})</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'attendance')}><p className="eyebrow">Présences</p><h2>Pointer un membre</h2><select name="eventId" required><option value="">Choisir un événement</option>{events.map(e=><option key={e.id} value={e.id}>{e.title}</option>)}</select><select name="memberId" required><option value="">Choisir un membre</option>{members.map(m=><option key={m.id} value={m.id}>{m.first_name} {m.last_name}</option>)}</select><select name="status"><option value="attended">Présent</option><option value="absent">Absent</option><option value="excused">Excusé</option></select><button disabled={busy} className="button button-dark">Enregistrer</button></form></div>}{n&&<p className="member-message">{n}</p>}<article className="panel"><p className="eyebrow">Calendrier</p><h2>Événements récents</h2><div className="finance-list">{events.map(e=><div key={e.id}><span><b>{e.title}</b><small>{new Date(e.starts_at).toLocaleString('fr-FR')} · {e.location||'Lieu à confirmer'}</small></span><b>{counts[e.id]||0} présent(s)</b></div>)}</div></article></div>}

"use client";import{FormEvent,useState}from'react';const modules=[['members','Membres'],['contributions','Cotisations'],['finance','Caisse & paiements'],['loans','Prêts & créances'],['solidarity','Solidarité'],['events','Événements'],['messages','Messages'],['reports','Rapports']];async function send(x:object){const r=await fetch('/api/organization',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function OrganizationManager({organization,modules:initial,settings,canManage}:{organization:any;modules:any[];settings:any;canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false),[mods,setMods]=useState(Object.fromEntries(initial.map(x=>[x.module_code,x.active])));async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{await send({action,...Object.fromEntries(new FormData(e.currentTarget))});setN('Paramètres enregistrés.');}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}async function toggle(code:string){setBusy(true);const active=!(mods[code]??true);try{await send({action:'module',code,active});setMods({...mods,[code]:active});setN('Module mis à jour.')}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace">{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'profile')}><p className="eyebrow">Profil</p><h2>Identité de l’organisation</h2><input required name="name" defaultValue={organization?.name}/><input name="phone" defaultValue={organization?.phone||''} placeholder="Téléphone"/><input name="email" defaultValue={organization?.email||''} placeholder="Email"/><input name="country" defaultValue={organization?.country_code||'CI'} placeholder="Pays"/><input name="currency" defaultValue={organization?.currency||'XOF'} placeholder="Devise"/><button disabled={busy} className="button button-dark">Enregistrer</button></form><article className="panel channel-note"><p className="eyebrow">Modules</p><h2>Fonctionnalités actives</h2><div className="module-list">{modules.map(([code,label])=><label key={code}><span>{label}</span><input disabled={busy} type="checkbox" checked={mods[code]??true} onChange={()=>toggle(code)}/></label>)}</div></article><form className="panel compact-form" onSubmit={e=>sub(e,'operations')}><p className="eyebrow">Opérations</p><h2>Règles de travail</h2><input name="timezone" defaultValue={settings?.timezone||'Africa/Abidjan'} placeholder="Fuseau horaire"/><input name="fiscalYear" type="number" min="1" max="12" defaultValue={settings?.fiscal_year_start||1} placeholder="Mois début exercice"/><input name="prefix" defaultValue={settings?.member_number_prefix||''} placeholder="Préfixe matricule"/><input name="paymentMethod" defaultValue={settings?.data?.default_payment_method||'cash'} placeholder="Paiement par défaut"/><button disabled={busy} className="button button-dark">Enregistrer</button></form></div>}{n&&<p className="member-message">{n}</p>}<article className="panel"><p className="eyebrow">Résumé</p><h2>Configuration actuelle</h2><div className="finance-list"><div><span><b>Type d’organisation</b><small>Configuration initiale choisie à l’onboarding</small></span><b>{organization?.organization_type}</b></div><div><span><b>Modules activés</b><small>Les modules désactivés restent masqués pour les utilisateurs.</small></span><b>{Object.values(mods).filter(Boolean).length}</b></div></div></article></div>}

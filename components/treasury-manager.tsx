"use client";import{FormEvent,useState}from'react';async function send(x:object){const r=await fetch('/api/treasury',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function TreasuryManager({accounts,categories,reconciliations,transactions,canManage}:{accounts:any[];categories:any[];reconciliations:any[];transactions:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false);async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{await send({action,...Object.fromEntries(new FormData(e.currentTarget))});setN('Opération enregistrée. Actualisez la page pour voir les données à jour.');e.currentTarget.reset()}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}const balance=transactions.reduce((x,t)=>x+(t.direction==='in'?Number(t.amount):-Number(t.amount)),0);return <div className="finance-workspace"><div className="finance-stats"><article><p>Comptes actifs</p><strong>{accounts.length}</strong></article><article><p>Solde global</p><strong>{balance.toLocaleString('fr-FR')} FCFA</strong></article><article><p>Rapprochements</p><strong>{reconciliations.length}</strong></article></div>{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'account')}><p className="eyebrow">1 · Comptes</p><h2>Ajouter un compte</h2><input required name="name" placeholder="Ex. Caisse principale"/><select name="type"><option value="cash">Espèces</option><option value="bank">Banque</option><option value="mobile_money">Mobile Money</option></select><button disabled={busy} className="button button-dark">Créer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'category')}><p className="eyebrow">2 · Catégories</p><h2>Classer les flux</h2><input required name="name" placeholder="Ex. Transport"/><select name="direction"><option value="out">Dépense</option><option value="in">Recette</option></select><button disabled={busy} className="button button-dark">Créer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'expense')}><p className="eyebrow">3 · Dépenses</p><h2>Enregistrer une dépense</h2><select required name="accountId"><option value="">Compte</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><input required name="category" placeholder="Catégorie"/><input required name="amount" type="number" min="1" placeholder="Montant"/><button disabled={busy} className="button button-dark">Enregistrer</button></form></div>}{n&&<p className="member-message">{n}</p>}<div className="finance-lists"><article className="panel"><p className="eyebrow">4 · Virements</p><h2>Entre comptes</h2><form className="compact-form" onSubmit={e=>sub(e,'transfer')}><select required name="from"><option value="">Depuis</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><select required name="to"><option value="">Vers</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><input required name="amount" type="number" min="1" placeholder="Montant"/><button disabled={busy} className="button button-dark">Virer</button></form></article><article className="panel"><p className="eyebrow">5 · Rapprochements</p><h2>Clôturer un compte</h2><form className="compact-form" onSubmit={e=>sub(e,'reconciliation')}><select required name="accountId"><option value="">Compte</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><input required name="book" type="number" placeholder="Solde comptable"/><input required name="actual" type="number" placeholder="Solde réel"/><button disabled={busy} className="button button-dark">Rapprocher</button></form></article></div></div>}

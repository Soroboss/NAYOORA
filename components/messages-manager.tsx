"use client";import{FormEvent,useState}from'react';async function send(x:object){const r=await fetch('/api/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function MessagesManager({members,messages,canManage}:{members:any[];messages:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false),[ids,setIds]=useState<string[]>([]);async function sub(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);try{await send({...Object.fromEntries(new FormData(e.currentTarget)),memberIds:ids});setN('Message créé. Les messages internes sont disponibles immédiatement ; emails et SMS restent en file d’envoi jusqu’au raccordement d’un prestataire.');e.currentTarget.reset();setIds([])}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace">{canManage&&<div className="finance-forms"><form className="panel compact-form message-form" onSubmit={sub}><p className="eyebrow">Nouveau message</p><h2>Composer</h2><select name="channel"><option value="internal">Notification interne</option><option value="email">Email</option><option value="sms">SMS</option></select><input name="subject" placeholder="Objet (optionnel pour SMS)"/><textarea name="body" required placeholder="Votre message aux membres…"/><div className="member-picker">{members.map(m=><label key={m.id}><input type="checkbox" checked={ids.includes(m.id)} onChange={()=>setIds(ids.includes(m.id)?ids.filter(x=>x!==m.id):[...ids,m.id])}/>{m.first_name} {m.last_name} <small>{m.email||m.phone||'sans contact'}</small></label>)}</div><button disabled={busy||!ids.length} className="button button-dark">Envoyer à {ids.length} membre(s)</button></form><article className="panel channel-note"><p className="eyebrow">Canaux</p><h2>Prêts au branchement</h2><p><b>Interne</b> — disponible immédiatement dans l’espace membre.</p><p><b>Email</b> — mis en file, à raccorder à Resend ou Brevo.</p><p><b>SMS</b> — mis en file, à raccorder à un fournisseur local.</p></article></div>}{n&&<p className="member-message">{n}</p>}<article className="panel"><p className="eyebrow">Historique</p><h2>Messages récents</h2><div className="finance-list">{messages.map(x=><div key={x.id}><span><b>{x.subject||x.body.slice(0,42)}</b><small>{x.channel} · {new Date(x.created_at).toLocaleString('fr-FR')}</small></span><b>{x.status}</b></div>)}</div></article></div>}

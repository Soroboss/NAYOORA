"use client";import{FormEvent,useState}from'react';const roles=['organization_admin','president','secretaire','tresorier','gestionnaire','membre'];async function send(x:object){const r=await fetch('/api/administration',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function AdministrationManager({organization,members,cards,users,invites,canManage}:{organization:any;members:any[];cards:any[];users:any[];invites:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false);async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{await send({action,...Object.fromEntries(new FormData(e.currentTarget))});setN('Mise à jour enregistrée. Actualisez la page pour voir la liste actualisée.');e.currentTarget.reset()}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace">{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'card')}><p className="eyebrow">Cartes de membre</p><h2>Émettre une carte</h2><select name="memberId" required><option value="">Choisir un membre</option>{members.map(m=><option key={m.id} value={m.id}>{m.first_name} {m.last_name}</option>)}</select><input name="prefix" placeholder="Préfixe, ex. MUT"/><button disabled={busy} className="button button-dark">Générer la carte</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'invite')}><p className="eyebrow">Utilisateurs</p><h2>Inviter un gestionnaire</h2><input required name="email" type="email" placeholder="email@organisation.org"/><select name="role">{roles.slice(1).map(r=><option key={r} value={r}>{r.replace('_',' ')}</option>)}</select><button disabled={busy} className="button button-dark">Créer l’invitation</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'settings')}><p className="eyebrow">Paramètres</p><h2>Organisation</h2><input required name="name" defaultValue={organization?.name}/><input name="phone" defaultValue={organization?.phone||''} placeholder="Téléphone"/><input name="email" defaultValue={organization?.email||''} placeholder="Email"/><input name="country" defaultValue={organization?.country_code||'CI'} placeholder="Pays (CI)"/><input name="currency" defaultValue={organization?.currency||'XOF'} placeholder="Devise"/><input name="timezone" defaultValue="Africa/Abidjan"/><button disabled={busy} className="button button-dark">Enregistrer</button></form></div>}{n&&<p className="member-message">{n}</p>}<div className="finance-lists"><article className="panel"><p className="eyebrow">Cartes</p><h2>Cartes émises</h2><div className="finance-list">{cards.map(c=><div key={c.id}><span><b>{c.member?.first_name} {c.member?.last_name}</b><small>{c.card_number}</small></span><b>{c.status}</b></div>)}</div></article><article className="panel"><p className="eyebrow">Accès</p><h2>Utilisateurs actifs</h2><div className="finance-list">{users.map(u=><div key={u.id}><span><b>{u.user_id.slice(0,8)}…</b><small>{u.status}</small></span>{canManage?<form onSubmit={e=>sub(e,'role')}><input type="hidden" name="membershipId" value={u.id}/><select name="role" defaultValue={u.role} onChange={e=>e.currentTarget.form?.requestSubmit()}>{roles.map(r=><option key={r} value={r}>{r}</option>)}</select></form>:<b>{u.role}</b>}</div>)}</div></article></div><article className="panel"><p className="eyebrow">Invitations</p><h2>En attente</h2><div className="finance-list">{invites.map(i=><div key={i.id}><span><b>{i.email}</b><small>{i.role} · {new Date(i.created_at).toLocaleDateString('fr-FR')}</small></span><b>{i.status}</b></div>)}</div></article></div>}

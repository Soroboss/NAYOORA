"use client";import{FormEvent,useState}from'react';async function send(x:object){const r=await fetch('/api/political',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function PoliticalManager({federations,sections,campaigns,canManage}:{federations:any[];sections:any[];campaigns:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false);async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{await send({action,...Object.fromEntries(new FormData(e.currentTarget))});setN('Enregistrement effectué. Actualisez la page pour voir la liste à jour.');e.currentTarget.reset()}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace"><div className="finance-stats"><article><p>Fédérations</p><strong>{federations.length}</strong></article><article><p>Sections locales</p><strong>{sections.length}</strong></article><article><p>Campagnes</p><strong>{campaigns.length}</strong></article></div>{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'federation')}><p className="eyebrow">Fédérations</p><h2>Créer une fédération</h2><input required name="name" placeholder="Nom de la fédération"/><input name="region" placeholder="Région"/><button disabled={busy} className="button button-dark">Créer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'section')}><p className="eyebrow">Sections locales</p><h2>Créer une section</h2><select name="federationId"><option value="">Sans fédération</option>{federations.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select><input required name="name" placeholder="Nom de la section"/><input name="locality" placeholder="Ville / commune"/><button disabled={busy} className="button button-dark">Créer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'campaign')}><p className="eyebrow">Campagnes</p><h2>Lancer une campagne</h2><input required name="name" placeholder="Nom de la campagne"/><select name="status"><option value="draft">Brouillon</option><option value="active">Active</option></select><input name="startsAt" type="date"/><input name="endsAt" type="date"/><button disabled={busy} className="button button-dark">Créer</button></form></div>}{n&&<p className="member-message">{n}</p>}<div className="finance-lists"><article className="panel"><p className="eyebrow">Territoires</p><h2>Fédérations & sections</h2><div className="finance-list">{federations.map(f=><div key={f.id}><span><b>{f.name}</b><small>{f.region||'Région non définie'}</small></span><b>{sections.filter(s=>s.federation?.name===f.name).length} section(s)</b></div>)}{sections.filter(s=>!s.federation).map(s=><div key={s.id}><span><b>{s.name}</b><small>{s.locality||'Localité non définie'}</small></span><b>Indépendante</b></div>)}</div></article><article className="panel"><p className="eyebrow">Campagnes</p><h2>Feuille de route</h2><div className="finance-list">{campaigns.map(c=><div key={c.id}><span><b>{c.name}</b><small>{c.starts_at||'Date à définir'} → {c.ends_at||'Date à définir'}</small></span><b>{c.status}</b></div>)}</div></article></div></div>}

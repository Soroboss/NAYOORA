"use client";import{FormEvent,useState}from'react';const f=(n:number)=>new Intl.NumberFormat('fr-FR',{style:'currency',currency:'XOF',maximumFractionDigits:0}).format(n);async function send(x:object){const r=await fetch('/api/ngo-impact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function NgoImpactManager({projects,budgets,volunteers,indicators,canManage}:{projects:any[];budgets:any[];volunteers:any[];indicators:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false);async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{await send({action,...Object.fromEntries(new FormData(e.currentTarget))});setN('Enregistrement effectué. Actualisez la page pour voir les données à jour.');((e.target || e.currentTarget) as HTMLFormElement | null)?.reset()}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace">{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'budget')}><p className="eyebrow">Budget</p><h2>Allouer un budget</h2><select required name="projectId"><option value="">Choisir un projet</option>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input required name="category" placeholder="Catégorie"/><input required name="amount" type="number" min="0" placeholder="Budget prévu FCFA"/><input name="actual" type="number" min="0" placeholder="Dépensé FCFA"/><button disabled={busy} className="button button-dark">Enregistrer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'volunteer')}><p className="eyebrow">Volontaires</p><h2>Ajouter un volontaire</h2><input required name="name" placeholder="Nom complet"/><input name="phone" placeholder="Téléphone"/><input name="skills" placeholder="Compétences séparées par virgules"/><button disabled={busy} className="button button-dark">Ajouter</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'indicator')}><p className="eyebrow">Impact</p><h2>Créer un indicateur</h2><select required name="projectId"><option value="">Choisir un projet</option>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input required name="name" placeholder="Nom de l’indicateur"/><input name="unit" placeholder="Unité"/><input name="target" type="number" min="0" placeholder="Cible"/><input name="current" type="number" min="0" placeholder="Valeur actuelle"/><button disabled={busy} className="button button-dark">Créer</button></form></div>}{n&&<p className="member-message">{n}</p>}<div className="finance-lists"><article className="panel"><p className="eyebrow">Budgets</p><h2>Par projet</h2><div className="finance-list">{budgets.map(b=><div key={b.id}><span><b>{b.project?.name} · {b.category}</b><small>Dépensé {f(b.actual_amount)}</small></span><b>{f(b.planned_amount)}</b></div>)}</div></article><article className="panel"><p className="eyebrow">Impact</p><h2>Indicateurs</h2><div className="finance-list">{indicators.map(i=><div key={i.id}><span><b>{i.name} · {i.project?.name}</b><small>{i.current_value} / {i.target_value} {i.unit||''}</small></span></div>)}</div></article></div></div>}

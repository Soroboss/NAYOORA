"use client";
import { FormEvent,useMemo,useState } from "react";
const money=(n:number)=>new Intl.NumberFormat('fr-FR',{style:'currency',currency:'XOF',maximumFractionDigits:0}).format(n);
async function send(body:object){const r=await fetch('/api/finance',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error);return d;}
export function FinanceManager({plans,members,contributions,payments,cash,canManage}:{plans:any[];members:any[];contributions:any[];payments:any[];cash:any[];canManage:boolean}){const[notice,setNotice]=useState('');const[busy,setBusy]=useState(false);const balance=useMemo(()=>cash.reduce((s,x)=>s+(x.direction==='in'?Number(x.amount):-Number(x.amount)),0),[cash]);async function submit(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);setNotice('');try{const f=new FormData(e.currentTarget);const d=Object.fromEntries(f);await send({action,...d});setNotice('Opération enregistrée. Actualisez la page pour voir les données à jour.');e.currentTarget.reset();}catch(e){setNotice(e instanceof Error?e.message:'Erreur.')}finally{setBusy(false)}}return <div className="finance-workspace"><div className="finance-stats"><article><p>Caisse disponible</p><strong>{money(balance)}</strong></article><article><p>Cotisations à encaisser</p><strong>{money(contributions.reduce((s,x)=>s+Number(x.amount_due)-Number(x.amount_paid),0))}</strong></article><article><p>Paiements récents</p><strong>{payments.length}</strong></article></div>{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>submit(e,'plan')}><p className="eyebrow">Cotisations</p><h2>Créer un plan</h2><input name="name" required placeholder="Ex. Cotisation mensuelle"/><input name="amount" required type="number" min="0" placeholder="Montant FCFA"/><select name="frequency"><option value="monthly">Mensuelle</option><option value="weekly">Hebdomadaire</option><option value="quarterly">Trimestrielle</option><option value="yearly">Annuelle</option></select><button disabled={busy} className="button button-dark">Créer</button></form><form className="panel compact-form" onSubmit={e=>submit(e,'contribution')}><p className="eyebrow">Échéance</p><h2>Attribuer une cotisation</h2><select name="memberId" required><option value="">Choisir un membre</option>{members.map(m=><option value={m.id} key={m.id}>{m.first_name} {m.last_name}</option>)}</select><select name="planId" required><option value="">Choisir un plan</option>{plans.map(p=><option value={p.id} key={p.id}>{p.name} — {money(p.amount)}</option>)}</select><input required name="dueDate" type="date"/><button disabled={busy} className="button button-dark">Créer l’échéance</button></form><form className="panel compact-form" onSubmit={e=>submit(e,'payment')}><p className="eyebrow">Paiements</p><h2>Encaisser</h2><select name="contributionId" required><option value="">Choisir une échéance</option>{contributions.filter(c=>c.status!=='paid').map(c=><option value={c.id} key={c.id}>{c.member?.first_name} {c.member?.last_name} — reste {money(c.amount_due-c.amount_paid)}</option>)}</select><input required name="amount" type="number" min="1" placeholder="Montant reçu FCFA"/><input name="provider" placeholder="Espèces, Wave, Orange…"/><button disabled={busy} className="button button-dark">Confirmer le paiement</button></form></div>}{notice&&<p className="member-message">{notice}</p>}<div className="finance-lists"><article className="panel"><p className="eyebrow">Cotisations</p><h2>Échéances récentes</h2>{contributions.length?<div className="finance-list">{contributions.map(c=><div key={c.id}><span><b>{c.member?.first_name} {c.member?.last_name}</b><small>{c.plan?.name} · échéance {c.due_date}</small></span><b>{money(c.amount_paid)} / {money(c.amount_due)}</b></div>)}</div>:<p className="muted">Aucune échéance pour le moment.</p>}</article><article className="panel"><p className="eyebrow">Caisse</p><h2>Derniers mouvements</h2>{cash.length?<div className="finance-list">{cash.map(x=><div key={x.id}><span><b>{x.category}</b><small>{new Date(x.occurred_at).toLocaleDateString('fr-FR')}</small></span><b className={x.direction==='in'?'positive':'negative'}>{x.direction==='in'?'+':'−'}{money(x.amount)}</b></div>)}</div>:<p className="muted">Aucun mouvement enregistré.</p>}</article></div></div>}

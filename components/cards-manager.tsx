"use client";import{FormEvent,useState}from'react';async function send(x:object){const r=await fetch('/api/cards',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function CardsManager({template,cards,canManage}:{template:any;cards:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false);async function sub(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);try{await send({action:'template',...Object.fromEntries(new FormData(e.currentTarget))});setN('Modèle enregistré.');}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}async function status(id:string,value:string){setBusy(true);try{await send({action:'status',cardId:id,status:value});setN('Statut de carte mis à jour. Actualisez la page.')}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace">{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={sub}><p className="eyebrow">Personnalisation</p><h2>Modèle de carte</h2><label>Couleur principale<input name="primary" type="color" defaultValue={template?.primary_color||'#183B32'}/></label><label>Accent<input name="accent" type="color" defaultValue={template?.accent_color||'#D8EE62'}/></label><label>Fond<input name="background" type="color" defaultValue={template?.background_color||'#FFFFFF'}/></label><input name="footer" defaultValue={template?.footer_text||''} placeholder="Texte de pied de carte"/><button disabled={busy} className="button button-dark">Enregistrer</button></form><article className="panel channel-note"><p className="eyebrow">Vérification</p><h2>QR sécurisé</h2><p>Chaque carte possède un lien de vérification unique. Il indique uniquement l’identité, l’organisation et le statut de validité.</p></article></div>}{n&&<p className="member-message">{n}</p>}<article className="panel"><p className="eyebrow">Cartes émises</p><h2>Registre</h2><div className="finance-list">{cards.map(c=><div key={c.id}><span><b>{c.member?.first_name} {c.member?.last_name}</b><small>{c.card_number} · <a href={`/card/${c.qr_token}`} target="_blank">Vérifier</a></small></span>{canManage?<button className="button" disabled={busy} onClick={()=>status(c.id,c.status==='active'?'blocked':'active')}>{c.status==='active'?'Bloquer':'Réactiver'}</button>:<b>{c.status}</b>}</div>)}</div></article></div>}

"use client";import{FormEvent,useState}from'react';async function send(x:object){const r=await fetch('/api/union',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function UnionManager({sectors,claims,mobilizations,canManage}:{sectors:any[];claims:any[];mobilizations:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false);async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{await send({action,...Object.fromEntries(new FormData(e.currentTarget))});setN('Enregistrement effectué. Actualisez la page pour voir la liste à jour.');e.currentTarget.reset()}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace"><div className="finance-stats"><article><p>Secteurs</p><strong>{sectors.length}</strong></article><article><p>Revendications ouvertes</p><strong>{claims.filter(c=>c.status!=='closed').length}</strong></article><article><p>Mobilisations</p><strong>{mobilizations.length}</strong></article></div>{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'sector')}><p className="eyebrow">Secteurs</p><h2>Créer un secteur</h2><input required name="name" placeholder="Ex. Enseignement secondaire"/><button disabled={busy} className="button button-dark">Créer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'claim')}><p className="eyebrow">Revendications</p><h2>Ouvrir un dossier</h2><select name="sectorId"><option value="">Tous secteurs</option>{sectors.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><input required name="title" placeholder="Objet de la revendication"/><input name="description" placeholder="Contexte"/><button disabled={busy} className="button button-dark">Ouvrir</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'mobilization')}><p className="eyebrow">Mobilisation</p><h2>Planifier une action</h2><select name="sectorId"><option value="">Tous secteurs</option>{sectors.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><input required name="title" placeholder="Nom de l’action"/><input name="date" type="datetime-local"/><input name="location" placeholder="Lieu"/><button disabled={busy} className="button button-dark">Planifier</button></form></div>}{n&&<p className="member-message">{n}</p>}<div className="finance-lists"><article className="panel"><p className="eyebrow">Revendications</p><h2>Dossiers</h2><div className="finance-list">{claims.map(c=><div key={c.id}><span><b>{c.title}</b><small>{c.sector?.name||'Tous secteurs'} · {c.opened_at.slice(0,10)}</small></span><b>{c.status}</b></div>)}</div></article><article className="panel"><p className="eyebrow">Mobilisation</p><h2>Actions prévues</h2><div className="finance-list">{mobilizations.map(x=><div key={x.id}><span><b>{x.title}</b><small>{x.sector?.name||'Tous secteurs'} · {x.location||'Lieu à confirmer'}</small></span><b>{x.status}</b></div>)}</div></article></div></div>}

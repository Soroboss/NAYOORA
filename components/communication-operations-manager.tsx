"use client";import{FormEvent,useState}from'react';async function send(x:object){const r=await fetch('/api/communications',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function CommunicationOperationsManager({templates,audiences,scheduled,members,canManage}:{templates:any[];audiences:any[];scheduled:any[];members:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false);async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{await send({action,...Object.fromEntries(new FormData(e.currentTarget))});setN('Enregistrement effectué. Actualisez la page pour voir les données à jour.');((e.target || e.currentTarget) as HTMLFormElement | null)?.reset()}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace">{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'template')}><p className="eyebrow">1 · Modèles</p><h2>Créer un modèle</h2><input required name="name" placeholder="Nom du modèle"/><select name="channel"><option value="email">Email</option><option value="sms">SMS</option><option value="whatsapp">WhatsApp</option><option value="internal">Interne</option></select><input name="subject" placeholder="Objet"/><textarea required name="body" placeholder="Contenu du message"/><button disabled={busy} className="button button-dark">Créer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'audience')}><p className="eyebrow">2 · Audiences</p><h2>Créer un segment</h2><input required name="name" placeholder="Nom de l’audience"/><input name="tag" placeholder="Tag membre (optionnel)"/><select name="status"><option value="active">Membres actifs</option><option value="inactive">Membres inactifs</option></select><button disabled={busy} className="button button-dark">Créer</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'preferences')}><p className="eyebrow">5 · Préférences</p><h2>Gérer un consentement</h2><select required name="memberId"><option value="">Choisir un membre</option>{members.map(m=><option key={m.id} value={m.id}>{m.first_name} {m.last_name}</option>)}</select><label className="check-line"><input name="email" type="checkbox" value="true" defaultChecked/> Email</label><label className="check-line"><input name="sms" type="checkbox" value="true" defaultChecked/> SMS</label><label className="check-line"><input name="whatsapp" type="checkbox" value="true" defaultChecked/> WhatsApp</label><button disabled={busy} className="button button-dark">Enregistrer</button></form></div>}{n&&<p className="member-message">{n}</p>}<div className="finance-lists"><article className="panel"><p className="eyebrow">3 · Planification</p><h2>Envois programmés</h2><div className="finance-list">{scheduled.map(s=><div key={s.id}><span><b>{s.subject||'Sans objet'}</b><small>{s.channel} · {new Date(s.scheduled_at).toLocaleString('fr-FR')}</small></span><b>{s.status}</b></div>)}</div></article><article className="panel"><p className="eyebrow">4 · Suivi</p><h2>Modèles & audiences</h2><div className="finance-list">{templates.map(t=><div key={t.id}><span><b>{t.name}</b><small>{t.channel}</small></span></div>)}{audiences.map(a=><div key={a.id}><span><b>{a.name}</b><small>Segment enregistré</small></span></div>)}</div></article></div></div>}

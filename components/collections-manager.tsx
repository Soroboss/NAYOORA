"use client";import{useMemo,useState}from'react';const f=(n:number)=>new Intl.NumberFormat('fr-FR',{style:'currency',currency:'XOF',maximumFractionDigits:0}).format(n);
export function CollectionsManager({contributions,imports,errors,canManage}:{contributions:any[];imports:any[];errors:any[];canManage:boolean}){const[n,setN]=useState(''),[filter,setFilter]=useState('all'),[busy,setBusy]=useState(false);const rows=useMemo(()=>filter==='all'?contributions:contributions.filter(c=>c.status===filter),[contributions,filter]);const overdue=contributions.filter(c=>c.status==='overdue');const outstanding=overdue.reduce((x,c)=>x+Number(c.amount_due)-Number(c.amount_paid),0);async function refresh(){setBusy(true);const r=await fetch('/api/collections',{method:'POST'}),d=await r.json();setBusy(false);setN(r.ok?`${d.updated} échéance(s) marquée(s) en retard. Actualisez la page.`:d.error)}function template(){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob(['Prénom;Nom;Téléphone;Email;Matricule\nAwa;Koné;+2250700000000;awa@example.com;M-001\n'],{type:'text/csv;charset=utf-8'}));a.download='modele-membres-NAYOORA.csv';a.click();URL.revokeObjectURL(a.href)}return <div className="finance-workspace"><div className="finance-stats"><article><p>Échéances suivies</p><strong>{contributions.length}</strong></article><article><p>Retards</p><strong>{overdue.length}</strong></article><article><p>À recouvrer</p><strong>{f(outstanding)}</strong></article></div><div className="finance-forms"><article className="panel channel-note"><p className="eyebrow">Import CSV avancé</p><h2>Qualité des données</h2><p>Téléchargez le modèle, importez depuis Membres et consultez les lignes rejetées ci-dessous.</p><button className="button button-dark" onClick={template}>Télécharger le modèle CSV</button></article><article className="panel channel-note"><p className="eyebrow">Retards</p><h2>Actualiser les impayés</h2><p>Transforme les échéances passées non soldées en retards, sans modifier les paiements validés.</p>{canManage&&<button disabled={busy} className="button button-dark" onClick={refresh}>{busy?'Analyse…':'Analyser les retards'}</button>}</article></div>{n&&<p className="member-message">{n}</p>}<article className="panel"><div className="directory-heading"><div><p className="eyebrow">États de paiement</p><h2>Échéances</h2></div><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">Tous les états</option><option value="due">À payer</option><option value="partially_paid">Partiel</option><option value="paid">Payé</option><option value="overdue">En retard</option></select></div><div className="finance-list">{rows.map(c=><div key={c.id}><span><b>{c.member?.first_name} {c.member?.last_name}</b><small>{c.plan?.name} · échéance {c.due_date} · {c.status}</small></span><b>{f(c.amount_paid)} / {f(c.amount_due)}</b></div>)}</div></article><div className="finance-lists"><article className="panel"><p className="eyebrow">Imports</p><h2>Derniers fichiers</h2><div className="finance-list">{imports.map(i=><div key={i.id}><span><b>{i.file_name}</b><small>{i.imported_rows}/{i.total_rows} importés · {i.failed_rows} erreur(s)</small></span><b>{i.status}</b></div>)}</div></article><article className="panel"><p className="eyebrow">Corrections</p><h2>Lignes à revoir</h2><div className="finance-list">{errors.map(e=><div key={e.id}><span><b>Ligne {e.row_number} · {(e.import as any)?.file_name}</b><small>{e.message}</small></span></div>)}</div></article></div></div>}

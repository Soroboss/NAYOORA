"use client";import{FormEvent,useState}from'react';async function send(x:object){const r=await fetch('/api/field-actions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(x)}),d=await r.json();if(!r.ok)throw Error(d.error);return d}
export function FieldManager({campaigns,sections,actions,canManage}:{campaigns:any[];sections:any[];actions:any[];canManage:boolean}){const[n,setN]=useState(''),[busy,setBusy]=useState(false);async function sub(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setBusy(true);try{await send({action,...Object.fromEntries(new FormData(e.currentTarget))});setN('Enregistrement effectué. Actualisez la page pour la liste à jour.');((e.target || e.currentTarget) as HTMLFormElement | null)?.reset()}catch(e){setN(e instanceof Error?e.message:'Erreur')}finally{setBusy(false)}}return <div className="finance-workspace"><div className="finance-stats"><article><p>Actions terrain</p><strong>{actions.length}</strong></article><article><p>Actions terminées</p><strong>{actions.filter(a=>a.status==='completed').length}</strong></article><article><p>Participants déclarés</p><strong>{actions.reduce((x,a)=>x+Number(a.participants_count||0),0)}</strong></article></div>{canManage&&<div className="finance-forms"><form className="panel compact-form" onSubmit={e=>sub(e,'field')}><p className="eyebrow">Action terrain</p><h2>Planifier une action</h2><select name="campaignId"><option value="">Sans campagne</option>{campaigns.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select name="sectionId"><option value="">Sans section</option>{sections.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><input required name="title" placeholder="Nom de l’action"/><input name="date" type="datetime-local"/><input name="participants" type="number" min="0" placeholder="Participants prévus"/><button disabled={busy} className="button button-dark">Planifier</button></form><form className="panel compact-form" onSubmit={e=>sub(e,'report')}><p className="eyebrow">Rapport terrain</p><h2>Clôturer une action</h2><select required name="actionId"><option value="">Choisir une action</option>{actions.filter(a=>a.status!=='completed').map(a=><option key={a.id} value={a.id}>{a.title}</option>)}</select><textarea required name="report" placeholder="Résultats, difficultés, prochaines étapes…"/><button disabled={busy} className="button button-dark">Enregistrer le rapport</button></form></div>}{n&&<p className="member-message">{n}</p>}<article className="panel"><p className="eyebrow">Suivi</p><h2>Actions récentes</h2><div className="finance-list">{actions.map(a=><div key={a.id}><span><b>{a.title}</b><small>{a.campaign?.name||'Hors campagne'} · {a.section?.name||'Territoire libre'} · {a.participants_count||0} participant(s)</small></span><b>{a.status}</b></div>)}</div></article></div>}
